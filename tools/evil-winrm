evil-winrm - The ultimate WinRM shell for pentesting 

Login with hash:
  evil-winrm -i <TARGET_IP> -u <USER> -H <HASH>

Login with password:
  evil-winrm -i <TARGET_IP> -u <USER> -p <PASSWORD>

Options:
  -i, --ip IP                      Remote host IP or hostname. FQDN for Kerberos auth (required)
  -u, --user USER                  Username (required if not using kerberos)
  -p, --password                   Password
  -H, --hash HASH                  NTHash
  -e                               Specify a location to pull binaries from. 
  -s                               Specify a script directory location
