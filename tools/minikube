Docs/Installation: https://minikube.sigs.k8s.io/docs/start/?arch=%2Flinux%2Fx86-64%2Fstable%2Fbinary+download
More information: https://github.com/kubernetes/minikube

Common commands:
  minikube start                        Start your cluster
  minikube start --driver docker        Start your cluster with docker as driver
  minikube stop                         Stop the running cluster
  minikube delete                       Delete the cluster
  minikube status                       Check status of the cluster
  minikube ip                           Get the IP address of the cluster
  minikube service <service>            To start a service in a browser

Other commands:
  minikube version                      Check version
  minikube docker-env                   To configure docker environment variables
  minikube service list                 To list all services
  minikube service --url service_name   To get the URL of a specific service
  minikube addons list                  List all the available add-ons
  minikube addons enable dashboard      Enable an addon from the list
  minikube dashboard                    Open the Kubernetes dashboard in a browser
  minikube dashboard --url              If you just want the dashboard URL, execute the following command
                                        It will start the proxy and displays the Kubernetes dashboard URL in the output

Profiles:
  minikube start -p dev                 You can have multiple minikube cluster instances in your workstation using minikube profiles
  minikube profile list                 List all the profiles using the list command
  minikube delete -p stage              Delete a minikube profile
  minikube profile dev                  Switch to the dev profile
  minikube profile                      Determine the current profile
  